<h1> Activity Form Data </h1>

<%= form_with scope: :fm_data, local: true do |fm_data| %>

<b> Select the Students data that you want to access </b>
<br>
<br>
<% option = options_from_collection_for_select(@athletes, :name, :name)%>
<%= fm_data.select :stdName, option%>

<br> <br>
<%= fm_data.submit "Show Data"%>
<br>
<br>

<table cellspacing="0" cellpadding="3">
  <tr>
    <th align = "left"> Name </th>
    <th align = "left"> Activity </th>
    <th align = "left"> Exertion </th>
    <th align = "left"> Date Time </th>
    <th align = "left"> Calculation </th>
  </tr>

  <% running_total = 0 %>
  <% @factivity.each do |factivity| %>
  <tr> 
    <td align = "left"><%= factivity.stdName %></td>
    <td align = "left"><%= factivity.activity %></td>
    <td align = "left"><%= factivity.exertion %></td>
    <td align = "left"><%= factivity.created_at.in_time_zone("Eastern Time (US & Canada)").strftime("%m/%d/%Y %H:%M")%></td>
    <td align = "left"><%=  %> </td>
  </tr>
  
  <% end %>
  <%= running_total %>
</table>

<%= link_to "Excel", products_path(
<% end %>
